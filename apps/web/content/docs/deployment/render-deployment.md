---
category: deployment
slug: render-deployment
title: Render Deployment (Recommended)
summary: Render is currently the safest production deployment target for Vista apps.
order: 2
updatedAt: 2026-03-17
---

> Render is currently the recommended production target for Vista.

If you are launching now, use Render first. This setup is written for end-users deploying a Vista app directly from project root.

## render.yaml Blueprint

```yaml title="render.yaml"
services:
  - type: web
    name: my-vista-app
    runtime: node
    region: oregon
    plan: free
    buildCommand: |
      npm install --no-audit --no-fund
      npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3003
    healthCheckPath: /
```

## Keep Free Render Service Awake

If your free Render service sleeps, use a scheduled GitHub Action ping.

```yaml title=".github/workflows/keep-render-awake.yml"
name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        env:
          RENDER_APP_URL: ${{ secrets.RENDER_APP_URL }}
        run: curl -sS -L "$RENDER_APP_URL/"
```

- Add a repository secret named `RENDER_APP_URL` with your live Render URL.
- Keep `healthCheckPath: /` in `render.yaml` so the root path is a safe ping target.
- The cron runs every 10 minutes (`*/10 * * * *`) to reduce free-tier cold starts.

## Why This Works

- Uses normal root-level scripts (`npm run build`, `npm run start`).
- Matches default scripts generated by `create-vista-app`.
- No internal source-directory commands required.

## Troubleshooting

- If modules are missing, ensure `npm install` runs in the same project root that contains your `package.json`.
- If CSS build fails, verify `postcss`, `postcss-cli`, and `tailwindcss` are installed and committed in lockfile.
- If start fails, confirm `build` and `start` scripts exist in your app `package.json`.

## Related

- [Vercel Deployment (Experimental)](/docs/deployment/vercel-deployment)
- [Vista Config Reference](/docs/reference/vista-config-reference)
