import { notFound } from 'vista/server'

export function generateMetadata() {
  notFound()
}

export default function SlotBoundaryModalSlotError() {
  return <p id="slot-boundary-modal-slot-error">slot-boundary-modal-slot-error</p>
}
